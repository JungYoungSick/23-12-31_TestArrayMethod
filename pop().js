const myArray = [1, 2, 3, 4, 5];
const lastElement = myArray.pop();
console.log(myArray);
// 기존의 값에서 마지막 배열의 값이 삭제되었다.
console.log(lastElement);
// 삭제된 마지막 배열의 값을 알려준다.

const list = [
  { name: "jung" },
  { age: 18 }
]
const lastList = list.pop();
console.log(list);
console.log(lastList);